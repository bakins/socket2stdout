#!/usr/bin/env bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd `dirname ${DIR}`

GOARCH=amd64 GOOS=linux CGO_ENABLED=0 go build ./cmd/socket2stdout

VERSION=`cat VERSION`

IMAGE="quay.io/bakins/socket2stdout:v${VERSION}"

docker build -t ${IMAGE} .
docker push ${IMAGE}
